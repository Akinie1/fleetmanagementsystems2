<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - Fleet</title>
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f4f7fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
            text-align: center; /* Center the text */
        }
        .container {
            border-radius: 10px;
            padding: 20px;
            width: 445px;
            position: relative;
        }
        .logo {
            margin-bottom: 20px;
        }
        .logo img {
            width: 140px;
        }
        h2 {
            color: #0b0f56;
            font-size: 48px; /* Increased font size */
            margin-bottom: 10px;
        }
        p {
            color: #666;
            font-size: 16px;
            margin-bottom: 30px;
        }
        .email-input {
            width: 100%;
        }
        .email-input input {
            width: 100%;
            padding: 15px;
            border-radius: 5px;
            font-size: 14px;
        }
        .email-input input::placeholder {
            color: #aaa;
        }
        button {
            width: 100%;
            padding: 15px;
            background-color: #0b0f56;
            border: none;
            border-radius: 5px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            margin-top: 20px;
            margin-bottom: 90px;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #090c4a;
        }
        #carousel-part {
    margin: 1rem;
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Adjust image size */
.rounded-image {
    width: 700px; /* Adjust size as needed */
    height: 580px;
    border-radius: 20px; /* Rounded bottom corners */
    margin: 5px;
    position: relative;
}

.rounded-image::before {
    content: '';
    position: absolute;
    top: -20px;
    left: 0;
    right: 0;
    height: 40px;
    background: linear-gradient(to bottom, rgba(255,255,255,0.7), transparent);
    border-top-left-radius: 50%;
    border-top-right-radius: 50%;
    filter: blur(10px);
}



        
        #submit {
            width: 100%;
            padding: 15px;
            background-color: #0b0f56;
            border: none;
            border-radius: 5px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            margin-top: 20px;
            margin-bottom: 90px;
            transition: background-color 0.3s;
        }
        #submit:hover {
        background-color: #F4ECFE;
        border: 1px solid black;
        color:#0B0F56;
        }
        @media screen and (max-width: 768px) {
            #carousel-part {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">
        </div>
        <h2>Forgot your Password?</h2>
        <p>Enter your email and we'll send you a <b><span style="font-weight: bolder;">link</span></b> to reset your password</p>
        
        <form action="./resetPassword" method="POST">
            <div class="email-input">
                <input type="email" name="email" placeholder="example@email.com" required>
            </div>
            <input type="submit" id="submit" value="Submit">
        </form>
    </div>
    <div id="carousel-part">
        <img class="rounded-image" src="car 4.png" alt="Fleet">
    </div>
</body>
</html>
