<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Password - TechChat</title>
    <style>
        :root {
            --blue: #090263;
            --background-color: #f4f7fa;
        }
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: var(--background-color);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
            text-align: center;
        }
        .container {
            text-align: left;
            max-width: 450px;
            padding: 20px;
        }
        .logo {
            margin-bottom: 20px;
        }
        .logo img {
            width: 140px;
        }
        h2 {
            color: var(--blue);
            font-size: 40px;
            margin-bottom: 10px;
        }
        p {
            color: #666;
            font-size: 16px;
            margin-bottom: 30px;
        }
        .form-row {
            margin-bottom: 20px;
        }
        .form-row input {
            width: 100%;
            padding: 15px;
            font-size: 16px;
            border: none;
            border-bottom: 2px solid var(--blue);
            background: none;
            color: var(--blue);
        }
        .form-row input:focus {
            border-bottom-color: #0b0f56;
            outline: none;
        }
        .form-row input::placeholder {
            color: #aaa;
        }
        #submit {
            width: 100%;
            padding: 15px;
            background-color: var(--blue);
            border: none;
            border-radius: 5px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        #submit:hover {
            background-color: #F4ECFE;
            border: 1px solid #0b0f56;
            color: #0b0f56;
        }
        #carousel-part {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-left: 50px;
        }
        #carousel-part img {
            max-width: 700px; /* Adjust the width of the image */
            height: auto;
        }
        @media screen and (max-width: 768px) {
            #carousel-part {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">
            <!-- Add logo if needed -->
        </div>
        <h2>Create a new password</h2>
        <p>Enter a new password below</p>
        
        <div id="sign-in">
            <form action="/createPassword" method="POST" autocomplete="off">
                <div class="form-row">
                    <input type="email" id="username" name="email" placeholder="email@gmail.com" required>
                </div>
                <div class="form-row">
                    <input type="password" id="password" name="password" placeholder="New Password" required>
                </div>
                <input type="submit" id="submit" name="form" value="Create Password">
            </form>
        </div>
    </div>
    <div id="carousel-part">
        <img src="/car 4.png" alt="Image of fleet cars">
    </div>
</body>
</html>
